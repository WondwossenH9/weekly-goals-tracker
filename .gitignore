# Flutter/Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
.metadata

# Secrets — never commit real Supabase keys
.env

# IDE
.idea/
*.iml
.vscode/

# Platform folders are generated locally via `flutter create` (see README)
# and intentionally not tracked here since they weren't hand-authored.
/android/
/linux/

# Generated code (drift's database.g.dart, etc.) — regenerate locally with
# `dart run build_runner build --delete-conflicting-outputs` per the README.
*.g.dart

# macOS
.DS_Store
